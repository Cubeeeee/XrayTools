namespace Xray.Tools.ExtractLib.Encode
{
    public enum EncodeType { None,RsaEncode,
        RsaDecode,
        HtmlEncode,
        HtmlDecode,
        UrlEncode,
        UrlDecode,
        UnicodeEncode,
        UnicodeDecode,
        Base64Encode,
        Base64Decode,
        MD5Encode,
        AESEncode,
        AESDecode,
    }
}
